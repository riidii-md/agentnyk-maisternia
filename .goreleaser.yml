version: 2

project_name: maisternia

before:
  hooks:
    - go mod verify

builds:
  - id: maisternia
    main: ./cmd/maisternia
    binary: maisternia
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    mod_timestamp: "{{ .CommitTimestamp }}"
    ldflags:
      - >-
        -s -w
        -X github.com/kagi-labs/agentnyk-maisternia/internal/buildinfo.Version={{ .Version }}
        -X github.com/kagi-labs/agentnyk-maisternia/internal/buildinfo.Commit={{ .Commit }}
        -X github.com/kagi-labs/agentnyk-maisternia/internal/buildinfo.Date={{ .CommitDate }}

archives:
  - id: release
    ids:
      - maisternia
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - README.md

checksum:
  name_template: checksums.txt
  algorithm: sha256

snapshot:
  version_template: "0.0.0-devel-{{ .ShortCommit }}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  prerelease: auto

homebrew_casks:
  - name: maisternia
    ids:
      - release
    binaries:
      - maisternia
    repository:
      owner: kagi-labs
      name: homebrew-tap
      token: '{{ envOrDefault "HOMEBREW_TAP_TOKEN" "" }}'
    directory: Casks
    homepage: https://github.com/kagi-labs/agentnyk-maisternia
    description: Opinionated, provider-neutral workshop for coding-agent harnesses
    caveats: |
      Run `maisternia` to open the configuration workshop. The versioned catalog
      installs automatically under ~/.config/maisternia; no source checkout is required.
    skip_upload: '{{ if eq (envOrDefault "HOMEBREW_TAP_TOKEN" "") "" }}true{{ else }}auto{{ end }}'
    custom_block: |
      require "download_strategy"

      class MaisterniaGitHubPrivateRepositoryReleaseDownloadStrategy < CurlDownloadStrategy
        def initialize(url, name, version, **meta)
          @url = url
          parse_url_pattern
          set_github_token
          meta[:headers] ||= []
          meta[:headers] << "Accept: application/octet-stream"
          meta[:headers] << "Authorization: Bearer #{@github_token}"
          ghurl = "https://api.github.com/repos/#{@owner}/#{@repo}/releases/assets/#{asset_id}"
          super(ghurl, name, version, **meta)
        end

        def download_url
          "https://#{@github_token}@api.github.com/repos/#{@owner}/#{@repo}/releases/assets/#{asset_id}"
        end

        private

        def parse_url_pattern
          pattern = %r{https://github.com/([^/]+)/([^/]+)/releases/download/([^/]+)/(\S+)}
          raise CurlDownloadStrategyError, "Invalid URL pattern" unless @url =~ pattern

          _, @owner, @repo, @tag, @filename = *@url.match(pattern)
        end

        def set_github_token
          @github_token = ENV["HOMEBREW_GITHUB_API_TOKEN"]
          unless @github_token
            raise CurlDownloadStrategyError, "HOMEBREW_GITHUB_API_TOKEN is required"
          end
          GitHub.repository(@owner, @repo)
        end

        def asset_id
          @asset_id ||= GitHub.get_release(@owner, @repo, @tag)["assets"]
                              .find { |asset| asset["name"] == @filename }
                              .fetch("id") { raise CurlDownloadStrategyError, "Asset not found" }
        end
      end
    url:
      template: "https://github.com/kagi-labs/agentnyk-maisternia/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
      using: MaisterniaGitHubPrivateRepositoryReleaseDownloadStrategy
      verified: github.com/kagi-labs/agentnyk-maisternia/
